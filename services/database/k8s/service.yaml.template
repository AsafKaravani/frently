apiVersion: v1
kind: Service
metadata:
  name: ${DEPLOYMENT_NAME}-postgres
  labels:
    app: ${DEPLOYMENT_NAME}-postgres
spec:
  type: LoadBalancer
  ports:
    - port: 5432
  selector:
    app: ${DEPLOYMENT_NAME}-postgres
