apiVersion: v1
kind: Service
metadata:
  labels:
    app: ${DEPLOYMENT_NAME}-hasura
  name: ${DEPLOYMENT_NAME}-hasura
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: ${DEPLOYMENT_NAME}-hasura
  type: LoadBalancer
